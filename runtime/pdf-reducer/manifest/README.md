# Runtime manifest

The deterministic production manifest is generated by `scripts/build-pdf-reducer.mjs`. It records runtime, qpdf, and Emscripten versions; artifact SHA-256 hashes and raw byte sizes; and the two supported modes. It deliberately contains no timestamp or machine path.
